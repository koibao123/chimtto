<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chia sẻ Màn Hình trên Android</title>
  <style>
    video {
      width: 100%;
      height: auto;
    }
  </style>
</head>
<body>
  <h1>Chia sẻ Màn Hình trên Chrome (Android)</h1>
  <button id="startShareBtn">Bắt đầu chia sẻ màn hình</button>
  <video id="sharedScreen" autoplay></video>
  
  <script>
    let localStream = null;

    const startShareBtn = document.getElementById("startShareBtn");
    const sharedScreen = document.getElementById("sharedScreen");

    startShareBtn.onclick = startScreenShare;

    async function startScreenShare() {
      try {
        // Yêu cầu quyền truy cập chia sẻ màn hình từ trình duyệt
        const stream = await navigator.mediaDevices.getDisplayMedia({
          video: true
        });

        // Hiển thị màn hình đã chia sẻ lên video element
        sharedScreen.srcObject = stream;
        
      } catch (err) {
        console.error("Lỗi khi chia sẻ màn hình:", err);
      }
    }
  </script>
</body>
</html>
